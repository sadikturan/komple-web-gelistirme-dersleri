import { InputEmailDirective } from './input-email.directive';

describe('InputEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEmailDirective();
    expect(directive).toBeTruthy();
  });
});
